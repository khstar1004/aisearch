# AI Search Widget

고객 쇼핑몰 HTML에 삽입하는 공용 widget asset 위치다.

현재 운영 widget은 `examples/HaeorumAISearch/widget/widget.js`에 있다. 제품화 전환 후 `widget/dist/widget.js`는 고객명 없는 `CommerceAISearch` global을 제공한다.

해오름 운영 호환을 위해 기존 `HaeorumAISearch` global alias는 일정 기간 유지한다.

